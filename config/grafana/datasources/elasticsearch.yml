apiVersion: 1

datasources:
  - name: Elasticsearch
    type: elasticsearch
    access: proxy
    url: http://elasticsearch:9200
    database: "pipeline-logs-*"
    jsonData:
      esVersion: "8.0.0"
      logMessageField: message
      logLevelField: level
      timeField: "@timestamp"
      interval: Daily
