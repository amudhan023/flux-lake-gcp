[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    integration: requires running infrastructure (Spark, MinIO, Kafka)
    slow: long-running benchmarks (>5 minutes)
junit_family = xunit2
